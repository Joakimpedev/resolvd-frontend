# Copy this file to `.env` and keep the URL as-is unless told otherwise.
# This value is embedded in the built app (it's an EXPO_PUBLIC_* var),
# so it's not a secret.
EXPO_PUBLIC_API_URL=https://resolvdapi-production.up.railway.app
